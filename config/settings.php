<?php

/** Uses this year for all database fields */
$config['gcs']['settings']['year'] = 2011;

/** Allows the submission of events */
$config['gcs']['settings']['submit_events'] = true;

/** Allows events to be viewed */
$config['gcs']['settings']['view_events'] = true;

/** Allow tickets to be purchased.  Applies only when event viewing is enabled. */
$config['gcs']['settings']['buy_events'] = true;

/** Allow event room and table to be viewed.  Applies only when event viewing is enabled. */
$config['gcs']['settings']['see_location'] = true;
